#spring:
#  datasource:
#    hikari:
#      jdbc-url: jdbc:h2:mem:testdb
#      driver-class-name: org.h2.Driver
#      username: test
#      password: test
#  jpa:
#    database-platform: org.hibernate.dialect.mysql
#    hibernate:
#      ddl-auto: create
#    show-sql: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test_ourmap?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace